// src-tauri/src/mcp/types.rs
// JSON-RPC 2.0 envelope + tool argument/result types. Filled in Phase F + G.

/// Placeholder so `mcp::mod.rs` re-export resolves. Phase F replaces this
/// with the real serde-deserializable JSON-RPC 2.0 request envelope.
pub struct JsonRpcRequest;

/// Placeholder so `mcp::mod.rs` re-export resolves. Phase F replaces this
/// with the real serde-serializable JSON-RPC 2.0 response envelope.
pub struct JsonRpcResponse;
