// src-tauri/src/mcp/error.rs
// MCP error enum + JSON-RPC error code mapping. Filled in Phase F.

/// Placeholder so `mcp::mod.rs` re-export resolves. Phase F replaces this
/// with the real error enum (variants + JSON-RPC code mapping).
pub struct McpError;
